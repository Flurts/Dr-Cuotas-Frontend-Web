query GetDoctorByStatus($status: String!) {
  getDoctorByStatus(status: $status) {
    user {
      first_name
      last_name
      profile_picture
      social_media {
        link
        status
        type
      }
    }
  }
}
